# PensiaMng server environment — העתק ל-.env ומלא ערכים אמיתיים
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/pensiamng?schema=public"
PORT=3210
# סוד לחתימת JWT — הפק מחרוזת אקראית: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="CHANGE_ME"
